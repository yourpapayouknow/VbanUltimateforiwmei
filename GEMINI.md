# VBAN Ultimate 项目标准提示词与工程规范 (GEMINI.md)

本文件是 **VBAN Ultimate for macOS** 项目的专属标准提示词与工程规范约束文件。在当前代码仓库中的任何操作、编码、重构、调试与界面构建，必须严格执行本文件包含的所有规则与约定。

---

## 一、全局执行环境与工程链路规范

### 1. 执行前置检查
- 任何会话开始或新任务推进前，必须先 analyze 本机全局规范文件 `/Users/mac/.gemini/GEMINI.md` 以及本提示词文件。

### 2. Shell 强制规范（macOS 本机环境）
- macOS 本机环境**唯一使用 Zsh (`/bin/zsh`)**。
- 标准命令执行调用形式：
  ```zsh
  /bin/zsh -lc '<command>'
  ```
- 脚本开头必须严格使用：
  ```zsh
  #!/bin/zsh
  ```
- **绝对禁止调用 `bash`、`/bin/bash`、`sh`、`/bin/sh`**，绝对禁止以任何理由自动回退到旧 Shell。
- 若涉及 macOS 原生应用或系统级别自动化，唯一使用 AppleScript。

### 3. 单一产物维护与继承原则
- 坚决维护本地唯一的应用产物：`build/VBANUltimate.app`。
- **严禁**为测试、对比或重构生成任何冗余、临时的新 App 副本，所有构建与拉起统一继承既有链路（`./Scripts/build_app.zsh`）。

### 4. Git 持续跟踪规范
- 所有代码与配置文件改动前，必须确保本地 Git 处于受控状态；
- 改动过程中按小步迭代、及时提交，每次提交附带清晰的 Conventional Commits 信息，确保可随时无损回滚。

### 5. 每轮对话 5W1H 记录规则
- 每轮对话结束后，必须在当前项目下的 `history/history-<chat编号>.md` 中追加记录该轮次的 5W1H（Who, What, When, Where, Why, How），防止上下文丢失。

### 6. 已有设置与端口修改影响探查规则
- 当涉及对系统已有设置、监听端口（如 UDP 6980）、网络流或驱动参数修改前，必须先探查是否与本地已有进程发生冲突（如 `lsof -iUDP:<port>`）；
- 若存在冲突或服务依赖，必须使用提问工具告知用户并等待确认，严禁擅自直接篡改或强杀外部进程。

---

## 二、代码编程与注释规范

### 1. 纯功能简明注释规范（红线约束）
- 每个函数、方法、模块、以及非封装的特定功能代码块最顶部，必须用一个**简明短语叙述功能**（如 `// 矩阵网格布局`、`// 滚动标签组件`）；
- **绝对永远禁止对于注释本身的解释性叙述**（如禁止出现“用于后续转换...”、“为了方便处理...”等解释性说明）；
- **绝对永远禁止在注释中夹带情绪化表达、对话历史引申或元叙述**。

### 2. 命名约束
- **永久禁止**拼音命名（如 `jiechusuoding` 等）；
- **永久禁止**英文全拼整句短语加下划线命名（如 `open_tab_via_button()`）；
- 统一遵循 Swift / macOS Cocoa API 官方命名风格或辅音行业标准简写。

### 3. 架构与依赖复用
- 严禁重复造轮子，已有工具、组件（如 `StatusLed`、`ParamCapsule`、`RollingLabel` 等）必须强制复用；
- 遵循单一原理实现原则，同一模块中严禁使用两种不同机制实现相同业务。

---

## 三、专业音频路由矩阵规范 (Audio Routing Matrix / Router)

### 1. 概念定位
- 本项目中的矩阵是**广播级音频路由矩阵（Routing Matrix / Crosspoint Matrix / Router）**；
- **严禁**将其理解为普通二维表格，也**严禁**默认理解为混音器（Mixer）。
- 信号链必须理解为：
  ```
  音频源 (Sources / Inputs) → Matrix Input → Crosspoint → Matrix Output → 目标端口 (Destinations / Outputs)
  ```

### 2. 坐标二维定义强约束
- **行（Row）= 输入信号源 (Input / Source)**：左侧纵向排列所有输入（物理输入、虚拟线缆输出端、VBAN RX）；
- **列（Column）= 输出目标端口 (Output / Destination)**：顶部横向排列所有输出（物理输出、虚拟线缆输入端、VBAN TX）；
- 每个网格交叉点表示一个由特定 Input 到特定 Output 的交叉连通点（Crosspoint）。

### 3. Router 路由行为语义
- **单目标端口源互斥 (Router Switch)**：
  - 同一个 Output（同一列）在同一时间严格只能连接一个 Input；
  - 当在同一列上点击另一个 Input 时，系统自动断开旧路由并建立新路由，实现广播级无缝切源；
- **广播分发 (Fan-out)**：
  - 同一个 Input（同一行）允许同时分发到多个不同的 Output 端口（同一行允许多个激活交叉点）。

### 4. 交叉点 (Crosspoint) 视觉形态
- **未连接状态**：方格保持微弱半透明底色（`Color.white.opacity(0.015)`）与细微边框，直接透出底层的点阵画布；
- **已激活状态**：
  - 方格采用青色主题高亮描边（`Theme.neonCyan` 2pt）；
  - **正中心居中显示粗体等宽大写 "ON" 字样**（`Theme.monoDigit(15, weight: .heavy)`）；
  - 严禁在单元格内堆砌多余文字、音量条或多余图标。
- **悬浮提示 (Tooltip)**：明确提示信号路径，如 `IN_NAME → OUT_NAME (已连接 · 点击断开)` 或 `(点击建立路由)`。

---

## 四、点阵画布与 4x4 点阵点对点对齐系统 (Point-to-Point Grid)

### 1. 核心数学模数
- 全局点阵基准步长：`dotStep = 24pt`；
- 画布点阵起始位置：`x = 12pt, y = 12pt`；
- 矩阵画布外侧补白：`gridPadding = 48pt`（即 $2 \times 24\text{pt}$）。

### 2. 标准方格 4x4 点阵尺寸强约束
- **一个标准方格必须严格等于 4x4 点阵大小**；
- 单格槽位（Slot）尺寸：$4 \times 24 = 96\text{pt}$；
- 内部方格（Inner Cell）尺寸：$92\text{pt} \times 92\text{pt}$；
- 外围均匀内缩 $2\text{pt}$，两相邻方格间隙严格为 $4\text{pt}$；
- **点阵对齐**：方格内部横向 4 个点、纵向 4 个点，**完整容纳 $4 \times 4 = 16$ 个圆点**；方格四周边框到最近点阵的距离严格保持对称的 **$10\text{pt}$**；
- **跨格贯通**：跨方格的点距为 $10\text{pt} (\text{边距}) + 4\text{pt} (\text{间隙}) + 10\text{pt} (\text{边距}) = 24\text{pt}$，全画布点阵在任何方格内部与方格外完全连续、等距，绝无错位。

### 3. 各标头组件的模数化
- **左侧输入端点标头**：宽度为 $9 \times 24 = 216\text{pt}$（内部 $212\text{pt}$），高度为 $4 \times 24 = 96\text{pt}$（内部 $92\text{pt}$）；
- **顶部输出端点标头**：宽度与高度均为 $4 \times 24 = 96\text{pt}$（内部 $92 \times 92\text{pt}$，即 $4 \times 4$ 点阵）；
- **外围虚线扩充槽位**：厚度为 $2 \times 24 = 48\text{pt}$（内部 $44\text{pt}$）。

---

## 五、端点标头交互与视觉排版规范

### 1. 虚线方格位置强约束
- **左侧与上侧严禁放置虚线方格**；
- 仅在矩阵**右侧**与**底侧**外围展示“+”号虚线方格（`MatrixDashedPlusCell`），用于扩充输出与输入通道。

### 2. 端点操作按钮规范
- 移除“物理输入/物理输出”等静态分类胶囊标签；
- **输入行按钮竖排**：`In 1..N` 标头右侧的操作按钮必须采用 **竖排（`VStack(spacing: 4)`）**，垂直居中排布在 $92\text{pt}$ 高度内，为左侧设备全称滚动标签释放横向展示宽度；
- **修改与删除按钮样式对齐**：样式完全对齐发送流卡片：
  - **修改按钮（铅笔 `pencil`，22x22）**：**必须加框**，拥有 $1.2\text{pt}$ 的青色圆角描边（`Theme.neonCyan.opacity(0.85)`）与高亮底色，点击呼出设备重选菜单；
  - **删除按钮（垃圾桶 `trash`，22x22）**：红色半透明图标，点击删除该通道并级联清除相关路由；
- 输入与输出标头中的修改按钮均必须保持显式描边框。

### 3. 标头文字与字号醒目化
- 剔除 `Out 1..N` 与 `In 1..N` 文字前置的扬声器、波形、麦克风等小图标；
- 序号文字放大至 **14pt 加粗等宽字体**（`Theme.monoDigit(14, weight: .bold)`）；
- 设备名称放大至 **12~12.5pt**；
- **设备全称展示**：采用 `RollingLabel` 横向滚动组件，长设备名称绝不截断混淆，辅以系统原生悬浮提示。

---

## 六、界面系统与交互减负准则

### 1. 去最外圈黑卡片化
- 矩阵网格（`matrixGridWrapper`）外层严禁包裹厚重黑卡片背景与外描边，矩阵方格直接裸露坐落在点阵画布上，点阵穿透方格。

### 2. 坐标交叉口留白
- 彻底移除左上角的 `cornerIndicator`（`OUT (DEST) ↗` / `↙ IN (SRC)`），原位保持透明留白。

### 3. 彻底消除说教式长句横幅
- 严禁在界面上放置任何居高临下、大段解释性的说教横幅；
- 所有功能提示与使用引导一律收敛至轻量原生 `.help(...)` Tooltip 中。

### 4. 一体化视图切换胶囊
- 顶部栏视图切换必须采用**一体化暗调滑块胶囊**（`@Namespace` + `.matchedGeometryEffect`），单个高亮块在暗调底槽内平滑滑动；
- 胶囊采用纯图标呈现（列表 `list.bullet` 与矩阵 `square.grid.2x2`），严禁生硬拼凑两个独立按钮。
